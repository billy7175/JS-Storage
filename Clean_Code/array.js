function getSomeValue(params){
  let tempVal = ''
  for (let index = 0; index < params.length; i++ ){
    const element = array[index]
  }

  if(temp ??){
    temp = ??
  } else {
    temp = ??
  }
}

// Bad Example